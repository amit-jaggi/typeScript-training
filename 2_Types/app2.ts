export { }

// Implicit type assignment or Type Inferencing

let numerical = 100;

let title = "Implicit type assignment or Type Inferencing";

let condition = false;

console.log(title, ":", typeof title);
console.log(numerical, ":", typeof numerical);
console.log(condition, ":", typeof condition);



let variableName;
console.log(variableName, ":", typeof variableName);

variableName = 200;
console.log(variableName, ":", typeof variableName);

variableName = "Changing value on same variable";
console.log(variableName, ":", typeof variableName);

variableName = true
console.log(variableName, ":", typeof variableName);